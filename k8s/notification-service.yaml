apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: order-processing

spec:
  replicas: 1

  selector:
    matchLabels:
      app: notification-service

  template:
    metadata:
      labels:
        app: notification-service

    spec:
      containers:
      - name: notification-service

        image: allan1804/notification-service:v1
        imagePullPolicy: Always

        envFrom:
        - configMapRef:
            name: order-config

        - secretRef:
            name: solace-secret
