apiVersion: keda.sh/v1alpha1
kind: TriggerAuthentication
metadata:
  name: solace-auth
  namespace: order-processing

spec:
  secretTargetRef:
  - parameter: username
    name: keda-solace-secret
    key: username

  - parameter: password
    name: keda-solace-secret
    key: password
